---
phase: 02-icons-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - layouts/partials/extend-head-uncached.html
  - layouts/partials/footer.html
autonomous: true
requirements:
  - ICON-01
  - ICON-02

must_haves:
  truths:
    - "Phosphor Icons bold/regular weight CSS가 CDN에서 로드되어 <i class='ph-bold ph-house'></i> 패턴이 아이콘으로 렌더된다"
    - "모바일 바텀 네비의 4개 이모지(🏠📰🏷️⬆️)가 Phosphor 벡터 아이콘으로 교체되어 OS별 렌더링 불일치가 해소된다"
    - "아이콘이 기존 .mp-nav-icon의 font-size: 1.2rem과 color를 상속받아 레이아웃이 유지된다"
  artifacts:
    - path: "layouts/partials/extend-head-uncached.html"
      provides: "Phosphor Icons CDN stylesheet links"
      contains: "phosphor-icons/web@2.1.2"
    - path: "layouts/partials/footer.html"
      provides: "Phosphor icon <i> tags replacing emoji <span> tags"
      contains: "ph-bold ph-house"
  key_links:
    - from: "layouts/partials/extend-head-uncached.html"
      to: "cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2"
      via: "<link rel='stylesheet'> tags"
      pattern: "phosphor-icons.*style\\.css"
    - from: "layouts/partials/footer.html"
      to: "layouts/partials/extend-head-uncached.html"
      via: "Phosphor CSS classes loaded by CDN stylesheet"
      pattern: "ph-bold ph-"
---

<objective>
Phosphor Icons v2 CDN을 연결하고 모바일 바텀 네비의 이모지 아이콘 4개를 Phosphor 벡터 아이콘으로 교체한다.

Purpose: OS별 이모지 렌더링 불일치 해소 + 통일된 아이콘 시스템 기반 마련 (향후 Phase 3~4에서도 Phosphor 아이콘 활용 가능)
Output: CDN stylesheet 2개 로드 + footer.html 이모지 → `<i>` 태그 교체
</objective>

<execution_context>
@C:/Users/sc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-icons-navigation/2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phosphor Icons v2 CDN stylesheet 추가 + 이모지 아이콘 교체</name>
  <files>
    layouts/partials/extend-head-uncached.html
    layouts/partials/footer.html
  </files>
  <action>
**1. extend-head-uncached.html에 Phosphor CDN 추가 (ICON-01)**

기존 `<link rel="stylesheet" href="{{ $fontsCssUrl }}">` 줄(7행) 바로 아래에 Phosphor Icons bold/regular weight CSS를 추가한다:

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/bold/style.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/regular/style.css">
```

- bold: 모바일 바텀 네비 + 향후 UI 아이콘용 (가시성 높음)
- regular: 향후 텍스트 인라인 아이콘용
- 전체 weight 로드(`<script>` 방식) 금지 — 3MB vs ~120KB 차이

**2. footer.html 이모지 → Phosphor 아이콘 교체 (ICON-02)**

`<!-- Mobile Bottom Nav -->` 섹션(73-90행)에서 이모지 `<span>` 4개를 `<i>` 태그로 교체한다:

| 현재 | 교체 후 |
|------|---------|
| `<span class="mp-nav-icon">🏠</span>` | `<i class="mp-nav-icon ph-bold ph-house" aria-hidden="true"></i>` |
| `<span class="mp-nav-icon">📰</span>` | `<i class="mp-nav-icon ph-bold ph-newspaper" aria-hidden="true"></i>` |
| `<span class="mp-nav-icon">🏷️</span>` | `<i class="mp-nav-icon ph-bold ph-tag" aria-hidden="true"></i>` |
| `<span class="mp-nav-icon">⬆️</span>` | `<i class="mp-nav-icon ph-bold ph-arrow-up" aria-hidden="true"></i>` |

주의사항:
- `aria-hidden="true"` 필수: 링크 텍스트(`<span>Home</span>` 등)가 이미 의미를 전달하므로 아이콘을 스크린리더에서 숨김
- `<span>` → `<i>` 태그 변경: Phosphor CSS가 `<i>` 태그의 `::before` pseudo-element로 아이콘 렌더
- `mp-nav-icon` 클래스 유지: 기존 CSS의 `font-size: 1.2rem`이 Phosphor 아이콘에 상속됨
- footer.html의 SVG 막대 그래프(47-57행)는 장식용 브랜드 비주얼이므로 교체하지 않음
- Phosphor regular weight 클래스는 `ph`(접두사만), bold는 `ph-bold`. `ph-regular` 같은 클래스는 존재하지 않으므로 사용 금지
  </action>
  <verify>
1. `hugo server` 실행 후 에러 없이 빌드 확인
2. 브라우저 DevTools Network 탭에서 `bold/style.css`와 `regular/style.css` CDN 로드 확인 (200 OK)
3. 모바일 뷰(640px 이하)에서 바텀 네비의 4개 아이콘이 Phosphor 벡터 아이콘으로 표시되는지 확인
4. 아이콘이 네모 박스나 깨진 문자가 아닌 정상적인 아이콘 형태로 렌더되는지 확인
  </verify>
  <done>
- extend-head-uncached.html에 Phosphor CDN `<link>` 2개가 존재한다
- footer.html에 이모지(🏠📰🏷️⬆️)가 0개이고 `ph-bold ph-*` 클래스를 가진 `<i>` 태그가 4개이다
- Hugo 빌드가 에러 없이 통과한다
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Phosphor CDN 링크 존재 확인
grep "phosphor-icons/web@2.1.2" layouts/partials/extend-head-uncached.html

# 2. 이모지 잔존 확인 (0건이어야 함)
grep -c "🏠\|📰\|🏷️\|⬆️" layouts/partials/footer.html

# 3. Phosphor 아이콘 태그 확인 (4건)
grep -c "ph-bold ph-" layouts/partials/footer.html

# 4. Hugo 빌드 검증
hugo --quiet 2>&1 | grep -i error
```
</verification>

<success_criteria>
1. `extend-head-uncached.html`에 Phosphor CDN stylesheet link가 bold/regular 각 1개씩 총 2개 존재
2. `footer.html`에 이모지 아이콘이 0개, Phosphor `<i>` 태그가 4개
3. 모든 `<i>` 태그에 `aria-hidden="true"` 속성 존재
4. Hugo 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/02-icons-navigation/02-01-SUMMARY.md`
</output>
